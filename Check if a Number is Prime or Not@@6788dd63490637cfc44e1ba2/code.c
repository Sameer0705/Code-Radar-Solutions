#include <stdio.h>

int main() {
    
    char a[100]={"Monady"};
    printf("%s",a[0]);
    // scanf("%d",&a);
    // (a%1==0&&a%a==0)?printf("Prime"):printf("Not Prime");    
    return 0;
}