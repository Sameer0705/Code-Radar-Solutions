#include <stdio.h>

int main() {
    int count=0;
    char str[100],d;
    scanf("%s\n",&str);
    scanf("%c",&d);
    for(int i=0;str[i]!='\0';i++)
    {
        if('d'==arr[i])
        {
            count++;
        }
    }
    printf("%d",count);
    return 0;
}